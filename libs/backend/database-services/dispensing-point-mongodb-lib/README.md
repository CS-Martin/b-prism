# dispensing-point-mongodb-lib

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test dispensing-point-mongodb-lib` to execute the unit tests via [Jest](https://jestjs.io).
